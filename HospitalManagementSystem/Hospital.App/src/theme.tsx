import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3d3195",
      light: "#5e60ce",
    },
  },
  typography: {
    fontFamily: "Roboto",
    fontSize: 17,
  },
});

export default theme;
